<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
     |  <%= user.id %>  |  <%= user.name %>  |  <%= user.email %>
      <%= link_to '詳細ページへ' , user_path(user.id) %>
    </li>
  <% end %>
  <%= paginate @users %>
</ul>
