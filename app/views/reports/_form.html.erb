<%= form_with model: report do |form| %>
  <%= form.label :title %>
  <%= form.text_area :title, size: '30x1', required: true %>
  <%= form.label :text %>
  <%= form.text_area :text, size: '30x10', required: true %><br>
  <%= form.submit %>
<% end %>
